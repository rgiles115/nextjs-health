// src/types/activityTypes.ts

export interface Activity {
    day: string;
    distance: number;
    average_watts: number;
    start_date: string;
    moving_time: number;
    total_elevation_gain: number;
}
